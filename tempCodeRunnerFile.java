  // public void insert(T key) {
    // if (head == null) {
    // Node newNode = new Node();
    // newNode.data = key;
    // head = newNode;
    // } else {
    // Node temp = head;
    // while (temp.next != null)
    // temp = temp.next;
    // Node newNode = new Node();
    // newNode.data = key;
    // temp.next = newNode;
    // }
    // }
